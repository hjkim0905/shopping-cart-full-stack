## Step 1

### 기능 구현 목록

- 상품 (Products Controller)
  - [x] 상품 목록을 조회할 수 있다. (GET /products)
    - [x] DB에 PRODUCTS 테이블이 존재하지 않으면 500 에러
    - [x] 성공시 200 OK
  - [x] 상품을 추가할 수 있다. (POST /products/)
    - [x] 성공시 201 Created
    - [x] DB에 PRODUCTS 테이블이 존재하지 않으면 500 에러
  - [x] 상품을 삭제할 수 있다. (DELETE /products/:id)
    - [x] DB에 PRODUCTS 테이블이 존재하지 않으면 500 에러
    - [x] 해당 상품이 장바구니에 있으면 함께 제거한다. (삭제될 상품 id에 해당하는 id를 포함하는 객체를 CART 테이블에서 삭제)
    - [x] 성공시 204 OK
    - [x] 해당 id가 디비에 존재하지 않는다면 404 에러

- 장바구니 (Cart Controller)
  - [x] 장바구니에 담긴 상품 목록을 조회할 수 있다. (GET /cart)
    - [x] 성공시 200 OK
    - [x] DB에 CART 테이블이 존재하지 않으면 500 에러
  - [x] 장바구니에 상품을 추가할 수 있다. (POST /cart/:id)
    - [x] 성공시 201 Created
    - [x] DB에 CART 테이블이 존재하지 않으면 500 에러
  - [x] 장바구니 상품의 수량을 변경할 수 있다. (PUT /cart)
    - [x] 성공시 204 OK
    - [x] 해당 id가 디비에 존재하지 않는다면 404 에러
    - [x] DB에 CART 테이블이 존재하지 않으면 500 에러
  - [x] 장바구니에 담긴 상품을 제거할 수 있다. (DELETE /cart/:id)
    - [x] 성공시 204 OK
    - [x] 해당 id가 디비에 존재하지 않는다면 404 에러
    - [x] DB에 CART 테이블이 존재하지 않으면 500 에러

- [x] 프론트엔드와의 연동을 위한 CORS 설정

### 검증

- [x] 필수 필드 누락 시 에러를 응답한다 (400 Bad Request)
- [x] quantity는 1 이상 99 이하의 정수여야 한다 (400 Bad Request)
- [x] price는 0보다 큰 숫자여야 한다 (400 Bad Request)
- [x] 상품명은 최대 100자이다 (400 Bad Request)

---

## Step 2

### 기능 구현 목록

- [ ] 불러오기
  - [x] `GET /cart` API 호출해서 장바구니 상품 목록 불러오기
  - [ ] 로딩중 일 때 스피너로 로딩 UI 표시하기
  - [ ] 에러 발생시 에러 메세지를 UI로 표시하기

- [x] 선택 상태 관리하기
  - [x] 진입 시 전체 선택이 기본값이다.
  - [x] 개별 상품을 선택 혹은 해제 가능하다.
  - [x] 전체 선택 혹은 해제 가능하다.
  - [x] 새로고침 후에도 선택 상태가 유지되어야한다.

- [ ] 결제 금액 / 배송비 (파생상태)
  - [ ] 선택된 상품들의 합산된 결제 금액을 동적으로 계산한다.
  - [ ] 결제 금액 10만원 이상 시 배송비 무료
  - [ ] 결제 금액이 10만원 미만일 시 배송비 3000원

- [ ] 수량 변경
  - [x] `PUT /cart` API 호출하기
  - [ ] 최소 1개, 최대 99개
  - [ ] 수량 변경 시 결제 금액에 즉시 반영한다.

- [x] 상품 삭제
  - [x] `PUT /cart` API 호출하기
  - [x] 삭제 후 목록을 갱신한다.

### 프로그래밍 요구사항

- [ ] 상태관리
  - [ ] useState / useReducer로 상태 구성하기
  - [ ] 필요 시 Context API로
  - [ ] 결제 금액, 배송비는 선택 상태에서 파생 계산하고 별도로 저장하지 않는다.

- [ ] 데이터 패칭 훅
  - [ ] data, loading, error을 반환하는 커스텀 훅을 구현한다. (라이브러리 사용금지)

- [ ] 코드 품질
  - [ ] 컴포넌트 / 비즈니스 로직 분리하기
  - [ ] refetchCart, deleteCartItem 같은 세부 구현 노출하는 props를 금지하고 DIP를 적용하기
